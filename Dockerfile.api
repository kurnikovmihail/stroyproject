# syntax=docker/dockerfile:1

FROM node:20-alpine

WORKDIR /app

COPY server/package.json ./
RUN npm install --omit=dev

COPY server/src ./src

ENV NODE_ENV=production
ENV API_PORT=3000

EXPOSE 3000

CMD ["npm", "start"]
